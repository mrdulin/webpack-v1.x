// const speak = require('./a');
// const b = require('./b');

// console.log('app', _.size([1,2,3,4,5]))
// speak();
// console.log('b', b.getLength());

//直接访问的api是http://www.google.com
//在浏览器url上添加查询字符串?env=cn，api是http://github.com
// var cModule = require('./c');

const _ = require('underscore');
console.log(_.size([1,2,3,4,5]));

alert(c);

// alert('api url is ' + autoconfig.api);
